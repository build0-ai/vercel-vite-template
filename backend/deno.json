{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --allow-read --watch main.ts",
    "build": "cd ../frontend && npm run build",
    "start": "deno run --allow-net --allow-env --allow-read main.ts",
    "check": "deno check **/*.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "test": "deno test",
    "validate": "deno task check && deno task lint && deno task fmt:check"
  },
  "imports": {
    "@oak/oak": "jsr:@oak/oak@^17.1.0",
    "@std/dotenv": "jsr:@std/dotenv@^0.225.0"
  },
  "compilerOptions": {
    "strict": false
  },
  "lint": {
    "rules": {
      "tags": [
        "recommended"
      ]
    }
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 80,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false,
    "proseWrap": "preserve"
  },
  "deploy": {
    "project": "617cab7b-044a-4fb4-a9f7-0d22b3ead48a",
    "exclude": [
      "**/node_modules"
    ],
    "include": [],
    "entrypoint": "main.ts"
  }
}